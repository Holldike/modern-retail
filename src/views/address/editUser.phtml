<h1>Edit Addresses</h1>
<h4>User: <?= $user->firstname ?> <?= $user->lastname ?></h4>

<?= $this->flash->output() ?>
<form method='post'>
    <p>
        <label>City</label>
        <?= $form->render('city') ?>
    </p>
    <p>
        <label>Region</label>
        <?= $form->render('region') ?>
    </p>
    <p>
        <label>Street</label>
        <?= $form->render('street') ?>
    </p>
    <p>
        <label>Postcode</label>
        <?= $form->render('postcode') ?>
    </p>
    <input type='submit' value='Add address'/>
</form>

<?php
if ($addresses->count() > 0) {
    ?>
    <table>
        <thead>
        <tr>
            <th>City</th>
            <th>Region</th>
            <th>Street</th>
            <th>Postcode</th>
            <th></th>
        </tr>
        </thead>
        <tbody>
        <?php foreach ($addresses as $address) { ?>
            <tr>
                <td><?= $address->city; ?></td>
                <td><?= $address->region; ?></td>
                <td><?= $address->street; ?></td>
                <td><?= $address->postcode; ?></td>
                <td><?= $this->tag->linkTo('address/delete/' . $address->address_id, 'Delete') ?></td>
            </tr>
        <?php } ?>
        </tbody>
    </table>
    <?php
}